let custom_style = `-- wimp.fight

SIGHT
  FIGHTER
    DISTANCE IS 1
      ATTACK TOWARD
    DEFAULT
      MOVE TOWARD
  OBJECT
    DISTANCE IS 1
      PUSH TOWARD
    DEFAULT
      STEP
  PROJECTILE
    DEFAULT
      STEP

DEFAULT
  TURN
    ID IS EVENT
      MOVE ANY
    ID IS ODD
      WAIT

MENU
  OPTION
    NAME IS AUDIO
      SET OFF
  CHARACTER
    NAME IS PATIENCE
      SELECT
  LOADOUT
    NAME IS TELEKINETIC
      SELECT

`;

let idle_style = `-- idle.fight

DEFAULT
  WAIT
`;
