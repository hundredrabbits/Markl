let custom_style = `-- wimp.fight

MENU
  OPTION
    NAME IS AUDIO
      SET OFF
  CHARACTER
    NAME IS PATIENCE
      SELECT
  LOADOUT
    NAME IS TELEKINETIC
      SELECT

SIGHT
  ENEMY
    DISTANCE IS 1
      DASH AWAY
      FIRE TOWARD
      STEP
    DISTANCE IS ANY
      FIRE TOWARD
    DEFAULT
      STEP
  OBJECT
    DISTANCE IS 1
      PUSH TOWARD
    DEFAULT
      STEP
  PROJECTILE
    DEFAULT
      STEP

DEFAULT
  TURN
    ID IS EVENT
      MOVE ANY
    ID IS ODD
      WAIT

`;

let idle_style = `-- idle.fight

DEFAULT
  WAIT
`;
