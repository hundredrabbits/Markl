let custom_style = `-- wimp.fight

SIGHT
  FIGHTER
    DISTANCE IS 1
      ATTACK TOWARD
    DEFAULT
      MOVE TOWARD
  OBJECT
    DISTANCE IS 1
      ATTACK TOWARD
    DEFAULT
      MOVE
  PROJECTILE
    DEFAULT
      STEP

DEFAULT
  DEFAULT
    DEFAULT
      TAUNT

MENU
  OPTION
    NAME IS AUDIO
      SET OFF
  CHARACTER
    NAME IS PATIENCE
      SELECT
  LOADOUT
    NAME IS TELEKINETIC
      SELECT

`;

let idle_style = `-- idle.fight

DEFAULT
  DEFAULT
    DEFAULT
      WAIT
`;
